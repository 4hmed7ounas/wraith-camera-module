# Core dependencies (lightweight versions for speed)
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Object Detection (YOLOv8 - nano model only for speed)
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# OCR (EasyOCR - lightweight, disabled by default)
easyocr>=1.7.0

# Utilities
scipy>=1.11.0
pyyaml>=6.0.0

# Performance monitoring
psutil>=5.9.0
